# Travis CI Configuration
# https://docs.travis-ci.com
# This file is based on
# TravisCI · koalaman/shellcheck Wiki
# https://github.com/koalaman/shellcheck/wiki/TravisCI
## Use container-based infrastructure for quicker build start-up
sudo: false

## We only support Ubuntu 14.04 and newer
dist: trusty

language: bash

## EXPERIMENTAL
## Customizing the Build - Travis CI
## https://docs.travis-ci.com/user/customizing-the-build/#Git-Clone-Depth
## > Please note that if you use a depth of 1 and have a queue of jobs, Travis CI won’t build commits that are in the queue when you push a new commit.
git:
  submodules: false
  depth: 1

addons:
  apt:
    packages:
    - realpath      # Merged to GNU Coreutils in Ubuntu 16.04

install:
- shellcheck --version

before_script:
- git submodule init
- git submodule update --depth=1 "Linters/GNU Bash Automatic Checking Program for Git Projects"

script:
- ./"Linters/GNU Bash Automatic Checking Program for Git Projects/GNU Bash Automatic Checking Program for Git Projects.bash"

matrix:
  fast_finish: true
